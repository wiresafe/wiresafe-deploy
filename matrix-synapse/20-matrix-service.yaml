apiVersion: v1
kind: Service
metadata:
  name: matrix
  namespace: matrix
  labels:
    app: matrix
spec:
  ports:
  - port: 80
    name: http
    targetPort: 8008
  - port: 8448
    name: federation
    targetPort: federation
  selector:
    app: matrix
  type: NodePort
